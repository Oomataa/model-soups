name: model_soups
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.11
  - pip
  - pytorch>=2.6.0
  - torchvision
  - pytorch-cuda=12.1
  - numpy
  - pandas
  - matplotlib
  - pillow
  - requests
  - pip:
    - ftfy
    - regex
    - tqdm
    - wget
